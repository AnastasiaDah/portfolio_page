import React from 'react';
import DropdownMenu from "./DropdownMenu";

const Header = () => {
    return (
        <div>
            header
            <DropdownMenu/>
        </div>
    );
};

export default Header;