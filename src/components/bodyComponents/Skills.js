import React from 'react';

const Skills = () => {
    return (
        <div>
            PostgresSQL, AWS, RabbitMQ - in progress
            JUnit, Mockito, RabbitMQ, Hibernate - need to download
            add ci/cd; agile; api; microservices; transactions
        </div>
    );
};

export default Skills;